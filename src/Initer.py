from src.Sender import *
from src.Piece import *

class Initer:

    @staticmethod
    def start(board):
        for i in range(board.size):
            for j in range(board.size):
                Sender.move( )